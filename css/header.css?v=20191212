/* Header */

.header-wrapper { width: auto !important; margin: 0 auto; }

.header { height: 75px; width: 100%; position: fixed; top: 0; z-index: 10; background-color: rgba(255,255,255,0.9); -webkit-font-smoothing: antialiased; -moz-osx-font-smoothing: grayscale; }
	/*.header.scrolled { border-bottom-color: rgba(0,0,0,0.06); }*/
	.header .logo { height: 74px; width: 74px; margin: 0; margin-right: 8px; padding: 12px; position: relative; float: left; display: inline-block; vertical-align: top; }
		.header .logo img { max-height: 100%; background-size: cover; }
			.header .logo:hover img { opacity: 0.8; }
				.has-background.header .logo:hover img { opacity: 1; }
		.header .logo a { position: absolute; top: 0; right: 0; bottom: 0; left: 0; z-index: 1; display: block; color: rgba(0,0,0,0); text-indent: -10000px; }

	.header .clipped { position: absolute; height: 50px; clip-path: circle(25.5px at center); }


	/* Main nav */

	.main-nav { margin: 0; display: inline-block; }
		.main-nav a { padding: 0 12px; display: inline-block; color: #999; cursor: pointer; float: left; font-size: 1rem; font-weight: 500; letter-spacing: -0.2px; line-height: 75px; text-decoration: none; -webkit-transition: color 0.1s ease; transition: color 0.1s ease; }
			.main-nav a:hover { color: #555; }
			.main-nav .active { color: #000 !important; }

		.has-background .main-nav a { color: rgba(255,255,255,1); }
			.has-background .main-nav a:hover { color: rgba(255,255,255,0.85); }


	/* Header has background */

	.has-background { background: none; }
		.has-background .logo:hover img { opacity: 1; }

		.has-background .main-nav a { opacity: 0.7; color: white; }
			.has-background .main-nav a:hover { opacity: 0.85; }
			.has-background .main-nav a:hover:active, .has-background .main-nav .active { opacity: 1 !important; color: white !important; }


	/* Top right */

	.top-right { margin-right: 12px; position: relative; float: right; }
		.top-right .available-on { position: absolute; top: 16px; left: 0; right: 0; z-index: 0; font-size: 10px; letter-spacing: 1px; opacity: 0.45; text-align: center; text-transform: uppercase; white-space: nowrap; }
			.has-background .available-on { color: #fff; opacity: 0.6; }

		.top-right a { height: 75px; padding: 34px 8px 0; position: relative; z-index: 1; display: inline-flex; flex-direction: row; justify-content: center; color: #111; cursor: pointer; font-size: 14px; font-weight: 400; line-height: 20px; opacity: 0.6; text-decoration: none; vertical-align: top; }
			.top-right a:hover { opacity: 0.75; }
			.has-background .top-right a { color: white; opacity: 0.85; }
				.has-background .top-right a:hover { opacity: 1; }
				.has-background .top-right svg { fill: #fff; }

			.top-right .available-links {  }
				.top-right .available-links a { outline: none; }

				.top-right .icon-browser { height: 20px; margin-right: 6px; }
				.top-right .icon-firefox { height: 21px; }



@media handheld and (max-width: 700px), screen and (max-device-width: 700px), screen and (max-width: 700px)  {

	.header {  }
		.header .logo {  }

		.main-nav { float: right; }
			.main-nav a:last-child { padding-right: 18px; }

		.top-right { display: none; }
}

